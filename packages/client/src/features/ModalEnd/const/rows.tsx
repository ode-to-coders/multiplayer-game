import { createData } from '../lib/createRows';

const rows = [
  createData(1, 'username', 20),
  createData(2, 'username', 18),
  createData(3, 'username', 15),
  createData(4, 'username', 13),
  createData(5, 'username', 12),
  createData(6, 'username', 10),
];

export default rows;
